scalar LocalDate

#  역할 및 권한(소매상1, 도매상2)
enum  AccountRole {
    # 소매상
    ROLE_RETAILER
    # 도매상 점주
    ROLE_WHOLESALER_EMPR
    # 도매상 직원
    ROLE_WHOLESALER_EMPE
}

# 거래처 신청 상태
enum ApplyStatus{
    # 신청중
    APPLYING,
    # 승인
    CONFIRMED,
    # 거부
    REJECTED,
}

schema{
    query: Query
}