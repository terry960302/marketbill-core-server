scalar LocalDate

#  역할 및 권한(소매상1, 도매상2)
enum  AccountRole {
    # 소매상
    RETAILER
    # 도매상 점주
    WHOLESALER_EMPR
    # 도매상 직원
    WHOLESALER_EMPE
}

# 거래처 신청 상태
enum ApplyStatus{
    # 신청중
    APPLYING,
    # 승인
    CONFIRMED,
    # 거부
    REJECTED,
}

# 꽃 등급
enum FlowerGrade{
    # 상
    UPPER,
    # 중
    MIDDLE,
    # 하
    LOWER,
}

enum Sort{
    # 오름차순
    ASCEND,
    # 내림차순
    DESCEND,
}

schema{
    query: Query
    mutation: Mutation
}